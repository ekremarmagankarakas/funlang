fun binarysearch(target, nums) {
  var length = len(nums);
  var l = 0;
  var r = length - 1;

  while l <= r {
    var mid = (l + r) / 2;
    to_int(mid);
    print(mid);
    if nums / mid == target {
      return target;
    }
    elif nums / mid < target {
      l = mid + 1;
    }
    else {
      r = mid - 1;
    }
  }
  return -1;
}

binarysearch(10, [3, 4, 10, 12, 14]);
