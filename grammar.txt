expr    : KEYWORD:VAR IDENT EQUALS expr
          term (('+' | '-') term)*

term    : factor (('*' | '/') factor)*

factor  : ('+' | '-')? factor
        : power

power   : atom ('^' factor)*

atom    : INT|FLOAT
        : LPAREN expr RPAREN

